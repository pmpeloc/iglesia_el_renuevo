Wide-tracked uppercase eyebrow that labels a slide — point number, series, or meeting.

```jsx
<Kicker>Punto 01 · Reunión General</Kicker>
```

- On cream use the default `var(--green-deep)`; on ink pass `color="var(--green)"`.
- Keep it to one short line; it sits directly above an Anton or Montserrat title.
